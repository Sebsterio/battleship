const Game = require("../src/game");

it("works in AIvAI", () => {
	expect(true).toBe(true);
	// const fakeUserInputFunction = () => {};
	// Array(5)
	// 	.fill(null)
	// 	.forEach(_ => {
	// 		const game = Game(1, "AIvAI", fakeUserInputFunction);
	// 		expect(["Player 1", "Player 2"]).toContain(game.resumeExecution());
	// 	});
});

// it('accepts user input', () => {
// 	const userInputFunction = (player, callback) => 'A1';
// 	expect(game(1, "AIvAI", userInputFunction))
// })
